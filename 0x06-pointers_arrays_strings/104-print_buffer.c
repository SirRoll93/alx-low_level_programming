#include "main.h"
#include <stdlib.h>

/**
 * print_buffer - prints buffer
 * @b: character pointed
 * @size: bytes of the buffer
 *
 * Return: Always 0
 */
void print_buffer(char *b, int size);
{
	char *b
}
